# git clone https://github.com/team-silvally/mitchell_sdc.git
sudo apt update
sudo apt install postgresql postgresql-contrib

#sudo -i -u postgres then psql
# OR to go directly into psql
sudo -u postgres psql
